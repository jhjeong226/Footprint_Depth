# PC Site Configuration

site:
  name: "PC"
  description: "PC observation site"
  bulk_density: 1.22  # 추정값 (데이터 확인 필요)
  field_capacity: 0.30
  
  location:
    latitude: null  # 데이터에서 읽어오기
    longitude: null
  
  soil_info:
    texture: "Sandy loam"  # 추정 (데이터 확인 필요)
    organic_matter: 0.22  # g/kg

analysis:
  max_extent: 150

dates:
  calibration:
    - "2024-07-23"
  analysis:
    - "2024-08-20"
    - "2024-09-20"
    - "2024-10-20"

interpolation:
  rbf_function: "thin_plate"  # "thin_plate" or "multiquadric"
  rbf_smooth: 0.1             # or 0.1

plotting:
  panel_swc:
    use_discrete_colors: true
    n_levels: 12
    cmap_name: "Blues"